/** "מי איתי" page title -- compact, matching the restrained header language from PR #9-#11. */
export function WithMeHeader() {
  return (
    <div className="min-w-0">
      <h1 className="text-2xl font-bold tracking-tight text-foreground sm:text-4xl">מי איתי</h1>
      <p className="mt-1.5 text-sm text-muted">הצוות סביב המשמרת שלך.</p>
    </div>
  );
}
