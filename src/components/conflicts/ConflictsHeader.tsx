/** "/conflicts" page title -- compact, matching the restrained header language from PR #9-#12. */
export function ConflictsHeader() {
  return (
    <div className="min-w-0">
      <h1 className="text-2xl font-bold tracking-tight text-foreground sm:text-4xl">התנגשויות ובדיקות</h1>
      <p className="mt-1.5 text-sm text-muted">דברים בסידור שלך שכדאי לבדוק.</p>
    </div>
  );
}
