/**
 * The product's display name is a TEMPORARY working label -- not final
 * branding. Every place that shows it in the UI imports it from here
 * instead of hardcoding the string, so renaming later is a one-line change
 * and never a find/replace across components.
 *
 * Do not build a logo or embed this name deeply into the visual identity;
 * treat it as plain text only.
 */
export const APP_NAME = "Luzly";
